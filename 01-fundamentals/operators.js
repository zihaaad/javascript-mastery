// 3. OPERATORS => are used to perform different types of methematical and logical computations. examples = + * >

// JavaScrpt Assignment ( = ) operator assigns a value to a variable
let x = 10;
let y = 90;

// JavaScript Addition ( + ) adds numbers
let z = x + y;

// JavaScript Multiplication ( * ) multiplies numbers
let multiply = x * y;

// There are differnt types of javascript operators:
/* 
    - Arithmetic 
    - Assignment 
    - Comparison 
    - String 
    - Logical 
    - Bitwise 
    - Ternary 
    - Type
*/

// Arithmetic +, -, *, **, /, %, ++, --
// Assignment =, +=, -=, *=, /=, %=, **=
// Comparison ==, ===, !=, !==, >, <, >=, <=, ?
// String <, +, +=
// Logical &&, ||, !
// Type typeof, instanceof
// Bitwise &, |, ^, ~, <<, >>, >>>
